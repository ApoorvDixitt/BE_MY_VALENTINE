[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "emergentintegrations"
version = "0.1.0"
description = "Emergent integrations for payment and LLM services"
requires-python = ">=3.7"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
